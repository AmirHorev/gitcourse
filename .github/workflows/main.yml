
name: course_action

on: 
  push:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest
  
    steps:
    - name: print hello
      run: echo "Hello"
      
    - name: print version of ubuntu
      run: lsb_release -a
      
    - name: print sha of commit
      run: github.sha
